<UserControl x:Class="RetailManagementSystem.Components.TopCategories"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RetailManagementSystem.Components"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:RetailManagementSystem.ViewModels"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d"
            >

    <Grid>
        <materialDesign:Card
            UniformCornerRadius="10"
            Width="500"
            Padding="20"
            Background="White">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Top 5 Categories by Products"
                           Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                           FontWeight="Bold"
                           Margin="0,0,0,10" />

                <Grid Width="Auto" Height="300" Grid.Row="1">
                    <lvc:CartesianChart 
                        Series="{Binding seriescollection}"
                        LegendLocation="Bottom">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis 
                                  LabelFormatter="{Binding formatter}"
                                  MinValue="0">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Step="50" />
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Labels="{Binding labels}"></lvc:Axis>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>

                </Grid>

                   
            </Grid>

        </materialDesign:Card>
    </Grid>

</UserControl>
