<Window x:Class="RetailManagementSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:RetailManagementSystem.ViewModels"
        xmlns:Menu="clr-namespace:RetailManagementSystem.Utils"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Retail Management Syste" Height="600" Width="850"
        Style="{StaticResource MaterialDesignWindow}"
        WindowStartupLocation="CenterScreen"
        Background="Transparent">

    <Window.DataContext>
        <vm:NavigationVM/>
    </Window.DataContext>
    
    <Grid>
        <!-- Window Border -->
        <Border Background="#212529" CornerRadius="20"/>

        <!-- Base Grid -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation Panel -->
            <Grid Grid.Column="0">
                <Border Background="#131B2F" CornerRadius="20,0,0,20"/>

                <StackPanel Height="400" Width="228" Margin="0,50,0,0">

                    <!-- Home -->
                    <Menu:Btn Style="{StaticResource BtnStyle}"
                              Command="{Binding HomeCommand}" 
                              IsChecked="True">
                        <Grid Height="50" VerticalAlignment="Bottom">
                          
                            <materialDesign:PackIcon 
                                Kind="HomeVariantOutline" 
                                Margin="28,0,0,0" 
                                VerticalAlignment="Center"
                                Height="24" Width="24" 
                                RenderTransformOrigin="0.037,0.341"
                                Foreground="White"/>
                            <TextBlock Text="Home"
                                       Style="{StaticResource Text_Style}"/>
                        </Grid>
                    </Menu:Btn>

                    <!-- Customers -->
                    <Menu:Btn Style="{StaticResource BtnStyle}" Command="{Binding CustomersCommand}">
                        <Grid>
                           
                            <materialDesign:PackIcon Kind="AccountOutline" Margin="28,0,0,0" VerticalAlignment="Center" Height="24" Width="24" RenderTransformOrigin="0.037,0.341" Foreground="White"/>
                            <TextBlock Text="Customers" Style="{StaticResource Text_Style}"/>
                        </Grid>
                    </Menu:Btn>

                    <!-- Products -->
                    <Menu:Btn Style="{StaticResource BtnStyle}" Command="{Binding ProductsCommand}">
                        <Grid>
                            <materialDesign:PackIcon Kind="ArchiveOutline" Margin="28,0,0,0" VerticalAlignment="Center" Height="24" Width="24" RenderTransformOrigin="0.037,0.341" Foreground="White"/>
                            <TextBlock Text="Products" Style="{StaticResource Text_Style}"/>
                        </Grid>
                    </Menu:Btn>
                    
                    <!-- Orders -->
                    <Menu:Btn Style="{StaticResource BtnStyle}" Command="{Binding OrdersCommand}">
                        <Grid>
                            <materialDesign:PackIcon Kind="InvoiceList" Margin="28,0,0,0" VerticalAlignment="Center" Height="24" Width="24" RenderTransformOrigin="0.037,0.341" Foreground="White"/>
                            <TextBlock Text="Orders" Style="{StaticResource Text_Style}"/>
                        </Grid>
                    </Menu:Btn>

                </StackPanel>
            </Grid>

            <!-- Content Area -->
            <Grid Grid.Column="1" Background="White">
                <ContentControl 
                  Content="{Binding CurrentView}"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Margin="0"/>
               
            </Grid>

           
        </Grid>
        
    </Grid>
</Window>
