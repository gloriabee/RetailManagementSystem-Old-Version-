<Window x:Class="RetailManagementSystem.Views.DynamicFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Add Item"
        Width="400"
        Height="Auto"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        ResizeMode="NoResize">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20"
                    materialDesign:ColorZoneAssist.Mode="PrimaryLight">

            
            <ItemsControl ItemsSource="{Binding Fields}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,10">
                            <ContentControl Content="{Binding}">
                                <ContentControl.Style>
                                    <Style TargetType="ContentControl">
                                        <Style.Triggers>

                                            <!-- TextBox -->
                                            <DataTrigger Binding="{Binding InputType}" Value="TextBox">
                                                <Setter Property="ContentTemplate">
                                                    <Setter.Value>
                                                        <DataTemplate>
                                                            <TextBox
                                                                Text="{Binding DataContext.FormData[{Binding BindingPath}], 
                                                                               RelativeSource={RelativeSource AncestorType=Window}, 
                                                                               Mode=TwoWay, 
                                                                               UpdateSourceTrigger=PropertyChanged}"
                                                                Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                                                materialDesign:HintAssist.Hint="{Binding Label}" />
                                                        </DataTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>

                                            <!-- DatePicker -->
                                            <DataTrigger Binding="{Binding InputType}" Value="DatePicker">
                                                <Setter Property="ContentTemplate">
                                                    <Setter.Value>
                                                        <DataTemplate>
                                                            <DatePicker
                                                                SelectedDate="{Binding DataContext.FormData[{Binding BindingPath}], 
                                                                                       RelativeSource={RelativeSource AncestorType=Window}, 
                                                                                       Mode=TwoWay}"
                                                                Style="{StaticResource MaterialDesignDatePicker}"
                                                                materialDesign:HintAssist.Hint="{Binding Label}" />
                                                        </DataTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>

                                            <!-- ComboBox -->
                                            <DataTrigger Binding="{Binding InputType}" Value="ComboBox">
                                                <Setter Property="ContentTemplate">
                                                    <Setter.Value>
                                                        <DataTemplate>
                                                            <ComboBox
                                                                ItemsSource="{Binding Options}"
                                                                SelectedItem="{Binding DataContext.FormData[{Binding BindingPath}], 
                                                                                       RelativeSource={RelativeSource AncestorType=Window}, 
                                                                                       Mode=TwoWay}"
                                                                Style="{StaticResource MaterialDesignComboBox}"
                                                                materialDesign:HintAssist.Hint="{Binding Label}" />
                                                        </DataTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>

                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                            </ContentControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Button Content="Submit"
                    Command="{Binding SubmitCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="0,20,0,0"
                    Height="40"
                    HorizontalAlignment="Right"/>
        </StackPanel>
    </ScrollViewer>
</Window>
