<UserControl x:Class="RetailManagementSystem.Views.OrderDetails"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RetailManagementSystem.Views"
             xmlns:components="clr-namespace:RetailManagementSystem.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="20,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left section -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="Order" FontSize="24" FontWeight="Bold" Foreground="#111" />
                <TextBlock Text="Created on " FontSize="14" Foreground="Gray"/>
            </StackPanel>

            
        </Grid>

        <StackPanel 
            Grid.Row="1"
            Orientation="Horizontal" 
            HorizontalAlignment="Center"
            Margin="0,0,10,0">
            <components:OrderInfoCard Margin="0,10,10,10"/>
            <components:CustomerInfoCard Margin="0,10,0,10"/>
        </StackPanel>

        <components:BillSummaryCard Grid.Row="2" Margin="10,10,10,10"/>


    </Grid>
</UserControl>
