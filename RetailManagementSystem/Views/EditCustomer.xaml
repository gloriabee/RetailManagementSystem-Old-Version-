<Window x:Class="RetailManagementSystem.Views.EditCustomer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RetailManagementSystem.Views"
        mc:Ignorable="d"
        Title="EditCustomer" 
        Height="400"
        Width="450"
        WindowStartupLocation="CenterScreen"
        Background="Transparent">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5,2"/>
        </Style>
    </Window.Resources>
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Username -->
        <Label Grid.Row="0" Grid.Column="0" Content="Username:" 
              VerticalAlignment="Center" Margin="0,5" FontWeight="Bold"/>
        <TextBox Grid.Row="0" Grid.Column="1" Height="30" 
                Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"
                Background="White" BorderThickness="1" BorderBrush="#E0E0E0"/>

        <!-- Email -->
        <Label
           Grid.Row="1" Grid.Column="0" Content="Email:" 
              VerticalAlignment="Center" Margin="0,5" FontWeight="Bold"/>
        <TextBox Grid.Row="1" Grid.Column="1" Height="30" 
                Text="{Binding Email,UpdateSourceTrigger=PropertyChanged}" Margin="0,5"
                Background="White" BorderThickness="1" BorderBrush="#E0E0E0"/>

        <!-- Phone -->
        <Label Grid.Row="2" Grid.Column="0" Content="Phone:" 
              VerticalAlignment="Center" Margin="0,5" FontWeight="Bold"/>
        <TextBox Grid.Row="2" Grid.Column="1" Height="30" 
                Text="{Binding Phone,UpdateSourceTrigger=PropertyChanged}" Margin="0,5"
                Background="White" BorderThickness="1" BorderBrush="#E0E0E0"/>

        <!-- Address -->
        <Label Grid.Row="3" Grid.Column="0" Content="Address:" 
              VerticalAlignment="Center" Margin="0,5" FontWeight="Bold"/>
        <TextBox Grid.Row="3" Grid.Column="1" Height="30" 
                Text="{Binding Address,UpdateSourceTrigger=PropertyChanged}" Margin="0,5"
                Background="White" BorderThickness="1" BorderBrush="#E0E0E0"/>

        <!-- Country -->
        <Label Grid.Row="4" Grid.Column="0" Content="Country:" 
              VerticalAlignment="Center" Margin="0,5" FontWeight="Bold"/>
        <TextBox Grid.Row="4" Grid.Column="1" Height="30" 
                Text="{Binding Country,UpdateSourceTrigger=PropertyChanged}" Margin="0,5"
                Background="White" BorderThickness="1" BorderBrush="#E0E0E0"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" 
                   HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Update"
                   Width="80"
                   Height="35"
                   Margin="0,0,10,0"
                   Command="{Binding UpdateCommand}"
                   CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                   Background="#0078D4"
                   Foreground="White"
                   BorderThickness="0"
                   Cursor="Hand" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
