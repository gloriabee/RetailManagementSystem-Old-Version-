<UserControl x:Class="RetailManagementSystem.Views.Products"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:RetailManagementSystem.ViewModels"
             xmlns:views="clr-namespace:RetailManagementSystem.Views"
             mc:Ignorable="d"
             Background="#212529">

    <UserControl.DataContext>
        <vm:ProductVM/>
    </UserControl.DataContext>

    <Grid>

        <views:GenericDataGrid
            ItemsSource="{Binding MyProducts}"
            ColumnsDefinition="{Binding ProductColumns}"
            AddCommand="{Binding ShowWindowCommand}"
            DeleteCommand="{Binding DeleteProductCommand}"
            DeleteAllCommand="{Binding DeleteAllCommand}"
            CurrentPage="{Binding CurrentPage}"
            TotalPages="{Binding TotalPages}"
            FirstPageCommand="{Binding FirstPageCommand}"
            PrevPageCommand="{Binding PrevPageCommand}"
            NextPageCommand="{Binding NextPageCommand}"
            LastPageCommand="{Binding LastPageCommand}"/>
       
    </Grid>
</UserControl>
